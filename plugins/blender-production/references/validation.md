# Validation scope

Validated on September 9, 2026 with Blender 5.1.2, Python 3.14 for external helpers, and local FFmpeg/ffprobe. Tests used a separate temporary directory and a fresh background Blender process without rendering or touching an existing project.

Passed observable checks:

- A valid six-frame 64×64 PNG sequence passes; missing frames, wrong dimensions, truncated files and changed chunk CRC fail.
- H.264 encoding produces six frames at 30 fps, passes stream and full decode checks, retains visual review as pending, and refuses to overwrite a finished video.
- Benchmark aggregation excludes cold and reused frames from warm timing and calculates a known remaining-time fixture correctly.
- In actual Blender, the probe returns the correct process identity, bounds object output, and detects a missing image and shared mesh.
- In actual Blender, the scoped bridge accepts inspection and an allowed project script; rejects wrong PID, wrong file and outside-directory scripts; and stops/releases its state when STOP is supplied. Timer callbacks were invoked explicitly in the isolated test; prolonged native UI timer scheduling and reconnection were not exercised by this smoke test.

This validation establishes helper behavior, not photoreal quality on every subject or compatibility with every Blender/MCP version. The material, lighting, viewport and performance guidance combines observed production outcomes with renderer-specific techniques that require a task-specific pilot. New scripts are original helpers, not copied provider implementation.

Use the installed-version API and test uncertain features in isolation. Web access to the primary Blender documentation returned fetch errors during skill creation; existing local research, installed source/API and actual Blender checks supported the version-specific guidance. No new remote MCP connection or installation was required for these tests.

The reflective-surface reference added on 2026-09-13 was checked against public artist tutorials and primary Blender, Autodesk, Adobe, and Marmoset documentation. Its comparison exercise is an adaptation for production game assets, not a completed car-model experiment or a formal surface-quality certification. Older tutorials support modeling principles; inspect installed-version controls before reproducing their UI steps.

Subsequent Blender 5.1.2 vehicle trials on the same date exercised paired whole-object and grazing renders, a moving reflection sequence, changed-versus-automatic native normals, retained guide inputs, and physical sections. Independent inspection found a local reflection improvement while larger form defects remained. These trials support the diagnostic method, not complete vehicle or formal curvature acceptance. Saved-record tests also reproduced a tuple/list inventory mismatch and verified explicit JSON-list capture with wrong-value, order and membership controls; that was a verifier defect, not damaged geometry. Reopen trials exposed a hidden driven-object initialization issue whose recovery must remain separate from normal cold-open delivery behavior.

Later trials reproduced small evaluated UV differences in a no-edit load/update/capture control while raw data stayed exact; the control did not exercise every candidate-specific operation. Separate finite-field checks found that retaining corner samples during retriangulation did not preserve the complete normal field. These observations support controlled attribution and interior-field comparisons. They do not establish a universal drift allowance or make a failed asset acceptable.

On 2026-09-14, an actual Blender 5.1.2 GLB contained an optional scalar specular extension that official Godot 4.7.1 ignored while reporting import success. The native material retained its default scalar; tracing its six LOD uses distinguished dynamically replaced instrument surfaces from unchanged static navigation surfaces. Separately, 38 exported-byte controls passed, including 34 corruption rejections and an explicit nonshipping second-UV fixture for a valid single-UV asset. These observations support material-path inspection and representative validation fixtures; full runtime adaptation, angular appearance and asset acceptance require their own evidence.

A subsequent actual Forward+ run measured 33 active material resources against a declared limit of 32 despite 26 authored material families. The native inventory included independent lamp channels and display/mirror materials. Only 13 first-LOD states completed before a combined brake/reverse request conflicted with the existing input policy. This supports native state-based accounting and reachable fixtures; the complete state maximum, damage contribution and optimization remained unverified at this checkpoint. Separately, all 66 focused scalar-adapter/wrapper cases and a clean actual-asset import passed; these results do not close angular appearance or resource-budget gates.

Later on 2026-09-14, all 60 reachable vehicle/LOD states completed. Consolidating three independently fed mirrors into one material reduced the measured peak from 34 to 32; 1,615 nonmirror native property snapshots retained their values after explicit process-identity normalization. Actual Forward+ and Compatibility captures compared the old and new mirror materials on the same meshes with frozen scene feeds and asymmetric midtones. Each renderer passed 15 positive pairs within one RGB code value and rejected a deliberately wrong horizontal mapping; independent feeds and resource recreation also passed. The rear scene feed still showed mostly cabin, leaving camera usefulness open. These checks support material equivalence and the measured resource limit for this candidate, with no material headroom; they do not establish final mirror usability, whole-vehicle performance or asset acceptance.

A subsequent host audit exercised 41 report/domain cases and 16 source-phase/binding cases. It exposed inadequate clearance bounds, an incomplete footprint count and a substituted moving-group identity that success labels and consistent hashes did not reject. Corrections rejected all five demonstrated bad-report witnesses while retaining the known native proof and valid host fixtures. This supports complete-domain report validation; the host fixtures do not establish a new saved-source, continuous-motion or visual acceptance result.

A later actual Blender 5.1.2 reopen left 16 hidden preview glyphs with identity world matrices despite intact stored transforms. Temporarily evaluating them visibly changed only their cached matrices; all complete raw hashes then matched the construction boundary and visibility was restored. Nine focused host controls exercised strict hash/geometry rejection and restoration after observation exceptions. The corrected native probe subsequently passed against the fresh saved source without relaxing its geometry or hash guards. This establishes the scoped observation correction, not normal cold-open behavior, final appearance or whole-asset acceptance.

Subsequent official Godot 4.7.1 captures exercised native serialization, camera transform normalization and full-resolution viewer framing. Full-precision JSON preserved the promoted binary32 values that abbreviated output had lost. A twelve-image 2560x1440 comparison exposed a false UI overlap from a combined hood/door rectangle; all 154 individual visible mesh envelopes were clear. The corrected reader retained whole-car window and 48px clearance rules, checked complete inventory coverage, and passed 15 focused controls. Independent inspection also found less shadow/edge noise at the measured High settings while broad black-panel and reflection defects remained. Separate day/night camera probes passed exact local/global transform and restoration checks but retained usefulness limitations. These observations support the capture methods, not final art, mirror usability, motion or performance acceptance.

An independent host audit of actual two-phase Blender generation records found that consistently rehashed command copies could omit all final outputs or name an unrelated final source. The unchanged native export binding still rejected those altered bindings; there was no new native or export pass. Role-based command validation then rejected both original counterexamples while preserving the two actual source ancestry positives and the previously bound export. Small host protocol tests covered complete unique outputs, every destination and specification-selected checkpoints. This supports command-to-artifact linkage, not newly executed Blender geometry, complete source QA or asset acceptance.

A later preparation helper passed syntax and preflight checks, then failed its post-build sealing branch because a loop replaced the source path variable with a filename string. Eleven complete CLI controls reproduced the original failure, exercised fresh staging, sealed both lighting configurations against an actual successful read-only build, and rejected malformed build/source records. The corrected helper then completed a fresh isolated build and preparation, followed by an official-engine parse. Its native grounding trial still failed separate runtime conditions. This supports exercising complete preparation branches and checking emitted artifact identities; it does not turn preparation success into native or asset acceptance.

An actual multi-instance Godot 4.7.1 trial then found that a legacy lamp adapter repeatedly reduced two shared source emission strengths despite a separate presenter owning active overrides. The changed sources had emission disabled, so the observation established shared-resource corruption without establishing visible dimming. A source-inclusive regression still passed until it explicitly switched the new and recreated instances' lamps off; that state reproduced the mutation. Restricting the legacy writer to assets without that presenter corrected both supported renderer trials while preserving the existing pixel and lifecycle guards. Separate actual legacy-asset and graybox checks exercised five on/off cycles and normal shutdown. These results support explicit material ownership and state coverage, not final lighting appearance or whole-vehicle acceptance.

A later finite receiving-wall trial found that a face mask omitted the triangle adjacent to a required displaced edge. Applying the intended movement to only the selected side would fold a narrow bevel strip. The repair scope was expanded to the complete connected receiving band with the same displacement, stock and clearance limits. This supports checking mask closure against the physical interface; the full replacement assembly and its visual acceptance were still pending.

On 2026-09-21, an isolated Godot 4.7.1 rear-view trial omitted the production sky presets and HDRIs and selected a procedural fallback. A fresh fixture retained the intended environment dependencies and recorded the actual panorama, exposure, lighting and nested viewport settings. With the mirror camera and materials unchanged, the sourced night environment made the following vehicles and illuminated road more legible in actual captures. Both native runs exited cleanly. This supports dependency and runtime-environment checks before visual tuning; it does not establish final-source visibility, idle-host performance or human usability approval.

A subsequent Godot 4.7.1 windowed probe reported stretched ViewportTexture getters of3413x1920 while both the actual window and disposed output image were2560x1440. Pinned engine source explained the extra stretch factor. Two image readbacks, outside the measured interval, took16.807ms and19.850ms; they were not claimed free or subtracted from acceptance totals. A separate headless run preserved null image observations and normal shutdown. Independent review verified the complete22-resource identity domain and both settings observations. The rendered wrapper still rejected timing for39percent background GPU utilization against its unchanged10percent ceiling. This supports getter-semantic and readback-lifetime checks, not accepted performance or final asset quality.

Later Blender 5.1.2 trials found two exact crossings after a rear-door mesh was baked into its existing hinge coordinates; the original passed, and the maximum world-coordinate roundtrip displacement was about0.119 micrometres. Repeating the check on all28 changed upper components isolated that door. The witness indices were local to an indexed shell and required mapping before selecting source faces. This supports testing the actual articulated output representation; the corrected door and full assembly clearance were still pending.

Separate matched renders traced an angular nose reflection to47 curved-skin fragments incorrectly classified as new flat walls. Restoring their original surface identity changed the normal field while preserving all physical geometry, UVs and material response. All ten paired images were inspected; the local sharp jut was removed while other visible form defects remained. Actual save/reopen/regeneration checked all three panels and rejected six altered inputs; both lower-detail tiers preserved the corrected fields. This supports physical field ownership and downstream verification, not complete vehicle or human visual acceptance.

Two later finite native-intersection coverage residuals remained unresolved after bounded adaptive subdivision, without an out-of-tolerance point. Exact partitioning against the original finite targets covered each whole residual with seven convex cells. Independent same-target distance and split/area replay found no remainder; the maximum bound was about0.601 micrometres under the unchanged1 micrometre limit. Fourteen controls covered gaps, missing targets, forged cells, malformed partitions and exhausted budgets. A synthetic-control naming error was retained and corrected before the actual proofs ran. No geometry or tolerance changed. This supports distinguishing an incomplete conservative proof from an observed physical defect; separate contact, stock, motion and whole-assembly acceptance remained open.

Blender5.1.2 distant-lid trials produced closed meshes with valid native encoding of the newly derived corner normals. Independent seven-point comparisons against corresponding original finite sheets nevertheless found shading differences exceeding49 degrees and53 degrees, including actual triangle interiors; these were not opposing-wall matches. The candidate geometry and original source remained unchanged during the audit, and no LOD policy or export was adopted from those results. This supports checking original interpolated fields separately from encoding accuracy; the samples were not a complete finite field bound or final visual judgment.
